import { getServer } from "../lib/utils"

describe("TodolistController e2e Test", () => {
  const app = getServer()
  const req = request(app)

  const rootApiPath = "/api/todolist"

  describe("", () => {})
})
